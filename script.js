const loader=document.querySelector('.loader');window.addEventListener('load',()=>setTimeout(()=>loader?.classList.add('hide'),650));
const nav=document.querySelector('.nav');document.querySelector('.menu')?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});reveals.forEach(e=>observer.observe(e));
const visual=document.querySelector('[data-tilt]');visual?.addEventListener('pointermove',e=>{if(innerWidth<800)return;const r=visual.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;visual.style.transform=`rotateY(${x*3}deg) rotateX(${-y*3}deg)`});visual?.addEventListener('pointerleave',()=>visual.style.transform='');
const features=[{k:'01 / SILENCE',t:'Hear<br>the <em>difference.</em>',p:'Our fan architecture moves air with a soft, steady rhythm. At night, it drops to just 19 dB — quieter than a whisper.'},{k:'02 / PURITY',t:'Breathe<br><em>cleaner.</em>',p:'A multi-stage filter captures fine particles and everyday pollutants, helping the air in your room feel noticeably fresher.'},{k:'03 / INTELLIGENCE',t:'Comfort<br>that <em>thinks.</em>',p:'Sensors learn the room around you and continuously tune airflow and temperature, so your setting stays effortless.'}];
const fk=document.querySelector('#featureKicker'),ft=document.querySelector('#featureTitle'),fp=document.querySelector('#featureText');document.querySelectorAll('[data-feature]').forEach(b=>b.addEventListener('click',()=>{const f=features[+b.dataset.feature];if(!fk||!ft||!fp)return;fk.textContent=f.k;ft.innerHTML=f.t;fp.textContent=f.p;document.querySelectorAll('[data-feature]').forEach(x=>x.classList.remove('active'));b.classList.add('active')}));
const form=document.querySelector('#contactForm'),note=document.querySelector('#formNote');form?.addEventListener('submit',e=>{e.preventDefault();if(note)note.textContent='Thanks — your consultation request is ready. We’ll be in touch shortly.';if(note)note.style.color='#1d7f3a';const btn=form.querySelector('button');if(btn){btn.innerHTML='Request received ✓';btn.disabled=true}});
const stage=document.querySelector('.feature-stage'),pin=document.querySelector('.feature-pin');window.addEventListener('scroll',()=>{if(!stage||!pin)return;const r=stage.getBoundingClientRect(),max=stage.offsetHeight-innerHeight,p=Math.min(1,Math.max(0,-r.top/max));const index=Math.min(2,Math.floor(p*3));const active=document.querySelector(`[data-feature="${index}"]`);if(active&&!active.classList.contains('active'))active.click();const img=pin.querySelector('.feature-image');if(img)img.style.background=`radial-gradient(circle at ${55+p*30}% ${42+p*10}%, #314a5a 0, #11181c 25%, #080808 58%)`});
const assembly=document.querySelector('.assembly-bg');const parts=[...document.querySelectorAll('.assembly-bg .ac-part')];const labels=[...document.querySelectorAll('.assembly-bg .assembly-label')];const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
function renderAssembly(){if(!assembly)return;const h=Math.max(document.documentElement.scrollHeight-innerHeight,1),p=Math.min(1,Math.max(0,scrollY/h));const eased=p<.5?2*p*p:1-Math.pow(-2*p+2,2)/2;const mobile=innerWidth<681;const s=mobile?.68:1;const states=[{x:0,y:0,z:0,r:0,sc:1,o:1},{x:-8,y:-28,z:0,r:-1,sc:1.03,o:1},{x:-13,y:-115,z:45,r:-2,sc:1.04,o:.98},{x:8,y:-125,z:0,r:1,sc:1.03,o:.98},{x:0,y:0,z:0,r:0,sc:1,o:1}];let q=eased*4;let i=Math.min(3,Math.floor(q)),t=q-i;const a=states[i],b=states[i+1],mix=v=>v+(b[v]-v)*t;const explode=Math.sin(Math.min(p,1)*Math.PI);const configs=[{x:0,y:-35*explode,z:90*explode,r:-2*explode,sc:1+explode*.05,o:1},{x:-22*explode,y:-170*explode,z:30*explode,r:-4*explode,sc:1+explode*.08,o:1},{x:18*explode,y:135*explode,z:70*explode,r:10*explode,sc:1+explode*.06,o:1},{x:-150*explode,y:105*explode,z:100*explode,r:-8*explode,sc:1+explode*.08,o:.95},{x:145*explode,y:-105*explode,z:110*explode,r:7*explode,sc:1+explode*.08,o:.95},{x:0,y:0,z:0,r:0,sc:1,o:1}];parts.forEach((el,n)=>{const c=configs[n]||configs[5];const x=c.x*s,y=c.y*s,z=c.z*s;el.style.transform=`translate3d(calc(-50% + ${x}px),calc(-50% + ${y}px),${z}px) rotate(${c.r}deg) scale(${c.sc})`;el.style.opacity=c.o;});assembly.classList.toggle('is-exploded',explode>.12);labels.forEach((l,i)=>{l.style.opacity=explode>.18?String(Math.min(1,(explode-.18)*2.5)):''});const hero=assembly.querySelector('.assembly-halo');if(hero)hero.style.transform=`scale(${1+explode*.28})`;
}
if(assembly){renderAssembly();let ticking=false;addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(()=>{renderAssembly();ticking=false});ticking=true}},{passive:true});addEventListener('resize',renderAssembly);}
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce){document.querySelectorAll('.button,.nav-button').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.06}px,${(e.clientY-r.top-r.height/2)*.06}px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});const cursor=document.createElement('div');cursor.className='cursor-glow';document.body.appendChild(cursor);addEventListener('pointermove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'})}
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const target=document.querySelector(a.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:reduce?'auto':'smooth',block:'start'})}}));
